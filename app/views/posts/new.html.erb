<% provide(:title, "投稿") %>
<% if @post.errors.any? %>
  <div id="error_explanation">
    <h2><%= @post.errors.count %>件のエラーがあります。</h2>
 
    <ul>
    <% @post.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
<p style="text-decoration: underline;"><%= link_to "利用規約", kiyaku_path %>をよく読んで内容に同意してから書き込んで下さい。</br>
卑劣な書き込み、出会いを求める書き込みは禁止していますので絶対にやめて下さい！</br>
悪質な投稿をする方は、投稿できないようにしています。</p>
<p><span class="kome">*</span>は必須項目です</p>
<%= form_for @post do |f| %>
  <div class="form-group">
  <label>ニックネーム<span class="kome">*</span></label>
	<%= f.text_field :name, placeholder: "ニックネーム", class: "form-control" %>
  </div>
  <div class="form-group">
  <label>本文<span class="kome">*</span></label>
	<%= f.text_field :description, placeholder: "本文", class: "form-control" %>
  </div>
  <div>
  <label>年齢</label>
	<%= f.select :age, @ages, {}, {class: "form-control"} %>
  </div>
  <div>
  <label>性別</label>
	<%= f.select :sex, @sex, {}, {class: "form-control"} %>
  </div>
  <div class="form-group">
  <label>都道府県</label>
	<%= f.select :prefecture, @prefectures, {}, {class: "form-control"} %>
  </div>
  <div class="form-group">
  <label>スナップチャットネーム</label>
	<%= f.text_field :snapchat_name, placeholder: "SnapChatネーム", class: "form-control" %>
  </div>
	<%= f.submit "投稿", class: "btn btn-primary" %>
<% end %>